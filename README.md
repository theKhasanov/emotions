# emotions

A simple React + Vite presentation app for emotional control and a mini quiz.

## Setup

1. Install dependencies:
   ```bash
   npm install
   ```
2. Run the development server:
   ```bash
   npm run dev
   ```
3. Open the local URL shown in the terminal.
